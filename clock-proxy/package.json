{
  "name": "dt-clock-proxy",
  "version": "1.0.1",
  "description": "Proxy de automação Playwright para os relógios Henry Hexa ADV — Delirio Tropical",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "jest --testEnvironment node",
    "install-browsers": "npx playwright install chromium"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "playwright": "^1.44.0"
  }
}
